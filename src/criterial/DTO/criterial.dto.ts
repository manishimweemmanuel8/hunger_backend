/* eslint-disable prettier/prettier */

import { Double } from 'typeorm';

export class CriterialDTO {
  marks_from: number;
  marks_to:number
  scholorshipId: string;
  combinationId:string;
  programId:string;
}
